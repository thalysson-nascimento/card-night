datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Card {
  id        String   @id @default(uuid())
  content   String
  createdAt DateTime @default(now())
}
